[project]
name = "codex-fastapi-wrapper"
version = "0.1.0"
description = "FastAPI wrapper around the OpenAI Codex CLI"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
]

[tool.uv]
package = false
