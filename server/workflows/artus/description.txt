Workflow: Artus (The Feature Tree)

Role: Semantic editor and AI agent interface.
Purpose: 
1. Parses the Python CAD script (`design.py`) using Python AST (Abstract Syntax Tree).
2. Extracts top-level variables and functions to create an editable "Feature List" in the UI.
3. Captures user changes to these features and wraps them into a prompt.
4. Uses an LLM to rewrite `design.py` based on the user's intent.

Shared Project Data: [tertius cache root]
- Watches: Nothing (it drives the changes).
- Modifies: `design.py`
